# packages/shared-types

Placeholder for TypeScript types/DTOs shared between `apps/web` and
`apps/api`. Left empty until a real type actually needs to be shared —
created now only so the workspace structure is visible from the start.
