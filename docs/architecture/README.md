# Architecture

High-level architecture overviews live here (diagrams, module boundaries,
data flow). Individual decisions with their reasoning live in
[`../adr/`](../adr/) instead — this folder is for describing the resulting
system, not for justifying each choice.

Populated starting Phase 1 as real modules exist to document.
